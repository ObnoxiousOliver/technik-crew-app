import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Technik Crew App',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
